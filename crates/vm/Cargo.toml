[package]
name = "vm"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8"
error = { path = "../error/" }
lexer = { path = "../lexer" }
parser = { path = "../parser" }
vir = { path = "../vir/" }
mir = { path = "../mir/" }
